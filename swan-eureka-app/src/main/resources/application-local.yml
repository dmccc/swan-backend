server:
  port: ${EUREKA_SERVER_PORT:8761} # 服务注册中心端口号

eureka:
  client:
    register-with-eureka: false # 是否向服务注册中心注册自己
    fetch-registry: false # 是否检索服务
  server:
    enable-self-preservation: false

logging:
  level:
    # 调测选项
    com.netflix.eureka: OFF
    com.netflix.discovery: OFF
